## Get Ready

> 📹 **Watch:** How colliderect() Works
> *Record in Screencastify — show bounding boxes, what overlap means, write an if statement to respond*
> *Objective: I can use colliderect() to detect when two sprites touch.*
> **[ADD VIDEO URL WHEN RECORDED]**

---

Before you run the code, look at this snippet from `collide.py`:

```python
if player.colliderect(alien):
    print("Hit!")
    exit()
```

**What do you think happens when this runs?** What does `colliderect` probably check?

> *[TODO: insert free-text-auto assessment — min 15 words — "What do you think colliderect() does? What happens when player and alien overlap?"]*
